source_datasources:
  - type: databricks
    query: >
      delete from chhs_emsa.gold.calmat_emsa_active_deployments_all_{{ env }} where effective_date = 
      (select max (ade_date_submitted) from chhs_emsa.silver.calmat_emsa_active_deployments_{{ env }});
destination_datasources:
  - type: noop