source_datasources:
  - type: databricks
    table: census.gold.county_language_{{ env }}

destination_datasources:
  # this is purposeful, we do not push to agol or anywhere at this point
  - type: noop
